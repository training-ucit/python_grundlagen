#!/usr/bin/env python3


tipp =[]
counter = 1
for  counter <= 6:
try:
    zahl = input("Bitte Zahl eingeben:")
    if zahl.isnumeric:
        n = 0
        note = True
        for n in tipp[n] <= len(tipp):
            if zahl == tipp[n]:
            note = False
               if note:
            tipp = tipp.append[zahl]

except:
        print ("keine Zahl")
 

 